---
layout: ../../layouts/MarkdownPostLayout.astro
title: "被广告打败了，从口袋记账转为一木记账"
author: Sean Feng
description: "因为口袋记账开始无休止广告模式，以及昂贵的会员去广告，让我开始认识了无广告的优秀记账App：一木记账"
pubDate: 2024-09-06
tags: ["记账软件", "一木", "AI","App","使用体验"]
# ## 高级样式

# ### 文字高亮

# 使用自定义的 Highlight 组件来实现文字高亮：

# <Highlight color="#25c2a0">这是绿色高亮文本</Highlight>
# <Highlight color="#1877F2">这是蓝色高亮文本</Highlight>


# ### 提示框
# 使用 Alert 组件来创建不同类型的提示框：
# <Alert type="info" title="提示">
#   这是一个信息提示框。
# </Alert>

# <Alert type="warning" title="警告">
#   这是一个警告提示框。
# </Alert>

# ### 可折叠内容

# 使用 Collapsible 组件来创建可折叠的内容块：
# <Collapsible client:load title="点击展开更多内容">
#   这是被折叠的内容。可以包含任何 Markdown 或 JSX 元素。
# </Collapsible>

# ### 标签页

# 使用 Tabs 组件来创建标签页：

# export const tabItems = [
#   {
#     label: "基本用法",
#     content: `
#       <p>这里是关于基本用法的内容。您可以包含任何Markdown或JSX元素。目前直接使用tabs组件会报错，所以暂时使用这个妥协方案。</p>
#       <ul>
#         <li>列表项1</li>
#         <li>列表项2</li>
#         <li>列表项3</li>
#       </ul>
#     `
#   },
#   {
#     label: "高级特性",
#     content: "<p>这里是关于高级特性的内容。</p>"
#   },
#   {
#     label: "注意事项",
#     content: `
#       <p>使用本组件时需要注意以下几点:</p>
#       <ol>
#         <li>确保正确导入组件</li>
#         <li>使用 client:load 指令以启用客户端交互</li>
#         <li>每个 Tab 都需要一个唯一的 label 属性</li>
#       </ol>
#     `
#   }
# ];

# <Tabs client:load items={tabItems} />

---

{/* 导入必要的样式和组件 */}
import '../../styles/markdown.css'
import React, { useState } from 'react';
import { Counter } from '../../components/react/Spinner';
import { Collapsible, Alert, CodeBlock, Highlight, FadeIn } from '../../components/react/Collapsible';
import { Tabs } from '../../components/react/Tabs';
import { Image } from 'astro:assets';

{/* import { MDXInlineCode } from '../../components/react/MDXFragmentCode'; */}

{/* 定义自定义样式 */}
<style>
{`
  @keyframes fadeIn {
    0% { opacity: 0; }
    100% { opacity: 1; }
  }
`}
</style>

{/* 显示文章标签 */}
<div className="flex justify-center flex-wrap gap-2 mb-4">
  {frontmatter.tags.map((tag) => (
    <span key={tag} className="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700">
      #{tag}
    </span>
  ))}
</div>

为了能够顺利的与Piksou启动项目，发兴重启去尝试注册一下claude。之前几次都卡在了接受短信验证码的环节，因为Cursor需要通过短信验证来判断具体账号的国家。试了各种免费的短信接收虚拟号都被判断是无效手机号。


在看了[别再傻傻开GPT-4会员了，Claude全面碾压GPT-4！手把手教你注册一个Claude 3.5账号并且开通会员！](https://mp.weixin.qq.com/s/rAQu6p3mO8HNHRQKSwTCSw)的推文介绍之后，使用了文章中说的5sim接受短信验证码。


在[https://5sim.net/zh/payment](https://5sim.net/zh/payment) 使用gmail登录之后，按照流程用支付宝充值了1美元，采购了一个英国的虚拟号完成整个注册过程。


之前在Cursor充分体验了Claude 3.5的能力，很多gpt都没有顺利实现的编程指令在claude3.5的加持下顺利编译通过。今天直接在官网试用的第一个感受就是，写代码的速度已经是直出的感觉，几乎没有任何等待的不适，但官网还只是说这是免费版，付费的还有更快更好的。这已经彻底解决了响应和品质的基本问题。


从用户交互角度上说，它通过弹出浮窗的方式解决了左侧导航窗的显示，当然它也可固定左侧导航，但文字对话页面的自适应显示似乎就有点问题，没有错开，出现了我发送内容和回答内容的重叠。

<image src="/SeanBlog/pictures/claude01.png" alt="claude"></image>

它灵活的将代码和网页显示在屏幕的右半部分，并不是像gpt那样选择了瀑布流式的嵌入在了对话的过程中。这样的好处就是可以切换代码显示和页面预览演示效果。类似各种网页代码说明的风格。

<image src="/SeanBlog/pictures/claude02.png" alt="claude"></image>


gpt和claude似乎都已经很好的实现了多模态图片文字的对齐，对发送给它的图片页面样式都可以很好的理解并且复刻或者查找其中的问题。cursor里面使用的就是这样的功能。但是cursor的代码补全，局部编译都是非常不错的功能，而且可以基于代码库等等。


但是都要$20每月啊~还是继续通过注册新邮箱薅cursor羊毛吧。



PS. 石墨文档的链接视图应该更新了，直接读取了文章的标题做成超链接，方便了一些。

